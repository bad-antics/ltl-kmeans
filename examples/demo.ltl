// examples/demo.ltl — runnable demo for ltl_kmeans

import std::io
use ltl_kmeans as lib

fn main() {
    io::println("ltl_kmeans demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}