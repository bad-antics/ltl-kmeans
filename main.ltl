// ltl-kmeans/main.ltl
//
// K-means++ clustering with elbow / silhouette scoring.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-kmeans v0.1.0")
    io::println("K-means++ clustering with elbow / silhouette scoring.")
    0
}
